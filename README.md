This is the data migration assignment ,where i generated the ecommerce data with 3 collections namely customers,sellers and the orders ad loaded that data to the mongodb which had upto 3-4 level nesting and arrays as values in that dictionary json format.Then after loading that to mongodb.I created the config.py file where i did the configuration of mongodb and mysql.After that created the extract.py where i extracted the data from the source taht is mongoDB and converted and if is dictionary then converted to list then tp dataframe,Then created transform.py file where i did the transformation on the data like converting the objectid of the mongodb to string format converting object formatted date-time fields to date like this did type conversion ,detected the list and exploded the list then also flatened the list then handled the null values and also sanitized the column names like.in column name is not accepted in mysql so replaced it with _.Then created the load file where i formed the connection with mysql using pysql  then did full load when there is no table exist it inserts the tansformed data in mysql after creating the table but when the data is already present then it does the incremental load ,here i used the is_active and row hash and last_updated concept,whenver we insert value to tale it checks if is_active is true(by default is_active=true when data is inseted),if true then check the composite key and hash value of that row that is hash of the entire row except is_active and if the hash value is different then that composite key having row is updated so we make is_active of that row as false and insert the updated value wih is_active as True.If the row hash is same for that composit key row then it is duplicate so no inserion ,If is_active is not present then it is new record insert it.This is how the incremental load works ,here as the composite key i took mongodb id which is i object id format that we converted to string and array index so that i is easier to find the arrays of the record updation or insert.Then in main.py we call the function that we defined in the each file.And can see the output.This is my approach for ETL.